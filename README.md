# My Custom Package

To install directly from GitHub, run:

```bash
pip install git+https://github.com/Greyzly/facades.git
```

Or you can install it from local folder and maintain 'editable' status to update and alter as you see fit:

```bash
pip install -e . #replace the dot with path if it is not currently selected
```